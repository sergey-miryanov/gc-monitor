# Python
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Pytest
.pytest_cache/
# tests
.temp

# Virtual environments
.venv/
.venv-l
venv/
env/
envs/

# OS / editors
.DS_Store
.idea/
.vscode/

# coverage
coverage*.xml
.coverage
.coverage*
htmlcov

# mypy
.mypy_cache

*.json
# ...except the traces under tests/fixtures, which are checked-in expectations
# rather than somebody's leftover run. See tests/monitoring/test_monitored_run_trace.py.
!tests/fixtures/*.json

examples/
# Anchored: `.github/scripts` is tracked.
/scripts/

# Local trace and capture artifacts. Anchored so a future `output/` inside the
# package is not swept up with them.
/output/

# Local issue tracker (see docs/agents/issue-tracker.md)
.scratch/
