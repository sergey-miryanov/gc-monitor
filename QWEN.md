## Qwen Added Memories
- In the gc-monitor project, monitoring functionality will be implemented using the _gc_monitor API (GCMonitorStatsItem, GCMonitorHandler, connect(), disconnect()) - not by directly using Python's gc module.
- The gc-monitor project uses CRLF line endings (Windows-style).
